# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# Editors / operating systems
.DS_Store
.idea/
.vscode/

# Local configuration and secrets
.env
.env.*
!.env.example

# The raw interaction file is 139 MB, above GitHub's 100 MB limit. The
# processed file is committed as data/processed_interactions.csv.gz instead,
# which train.py reads directly; the uncompressed copy is a local convenience.
# Model artifacts and training reports are committed so results can be checked.
data/interactions.csv
data/processed_interactions.csv

# Logs
*.log

# Scratch from an in-place rebuild; safe to remove
_to_delete/
