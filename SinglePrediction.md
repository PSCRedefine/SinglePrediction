## 1. 项目预览

**项目名称**：Cognitive Shorts Recommendation System

**背景**：平台存在用户与视频的交互数据（观看、点赞、评论、分享等），构建一个的端到端 ML 系统：

- 离线：对原始 CSV 做数据处理，生成训练用特征表；
- 训练：用特征表训练多种模型并选择最佳；
- 在线：前端页面收集少量输入 → 后端推导完整特征 → 调用模型输出互动概率与置信度

**核心目标**

1. 生成 `processed_interactions.csv`（训练用特征样本表）
2. 使用该表训练 4 个模型：逻辑回归、随机森林、梯度提升树、LightGBM
3. 提供一个首页（Single Prediction）能输入必要字段，展示预测概率、置信度、耗时

## 2. 业务目标

围绕 视频推荐预测 场景，交付一个“单条样本预测”页面，让产品、算法、前端、后端都可以基于统一输入快速验证模型服务的可用性

- 项目基础工程搭建
- 前端主入口与页面切换框架
- FastAPI 后端服务基础能力
- 模型加载与单条预测接口
- Single Prediction 页面交互与结果展示
- 前后端联调验证
- 实现基础输入校验、异常提示、结果可视化展示
- 为后续批量预测、分析面板、模型信息页预留统一入口和扩展方式

### 2.1 技术栈

- 语言：Python
- 前端：Streamlit
- 后端：FastAPI
- 数据处理：Pandas / Numpy
- 模型：scikit-learn + LightGBM
- 数据校验：Pandera

## **3. 数据集说明**

### 3.1 输入数据

1. `users.csv`：用户画像/账号信息/统计
2. `videos.csv`：视频元数据/内容特征/统计
3. `interactions.csv`：交互流水（每行一次用户-视频行为）

### 3.2 构建模型训练数据

`processed_interactions.csv`：训练用特征表（每行一个样本）

## 4. 任务拆解

### 4.1 任务一：项目工程骨架搭建

目标：让项目具备统一的目录结构、依赖管理方式和运行入口

建议交付项：

- `src/` 作为核心源码目录
- `docs/` 作为文档目录
- `models/` 作为模型与推理工件目录
- `monitoring/` 作为监控配置目录
- `scripts/` 作为初始化与辅助脚本目录
- `requirements.txt` 作为依赖清单

### 4.2 任务二：后端基础服务搭建

目标：交付一个可独立运行的模型推理 API

技术选型：

- Web 框架：FastAPI
- 服务启动：Uvicorn
- 数据校验：Pydantic
- 模型加载：`joblib`

后端需要完成：

- 服务启动与健康检查
- 跨域配置，保证本地联调顺畅
- 模型加载与特征列加载
- `POST /predict` 单条预测接口
- 错误处理与统一返回

### 4.3 任务三：前端基础搭建

目标：让前端具备页面入口、侧边栏、调用后端 API 的通用能力

技术选型：

- 前端框架：Streamlit
- HTTP 调用：`requests`
- 图表展示：Plotly

前端需要完成：

- 应用主入口
- 页面布局初始化
- 侧边栏导航
- 通用 API 调用函数
- 服务健康状态提示

### 4.4 任务四：完成 Single Prediction 页面

目标：交付第一个可以操作的用户页面

本页面内容：

- 收集单条预测需要的输入参数
- 调用后端预测接口
- 展示预测结果与基础解释信息
- 对明显非法输入做前端拦截
- 提供错误反馈，帮助定位问题

## 5. 一阶段功能设计

### 5.1 页面名称

`Single Prediction`

### 5.2 页面目标

为单个“用户-视频”请求生成一次推荐/参与度预测结果，作为整个系统的最小可运行演示入口

### 5.3 页面输入

坚持“最小必要输入”原则，不要求前端一次性采集训练期全部特征，而是只收集在线请求最关键的字段

- 在线请求通常只传最少业务字段
- 其余特征可由后端补齐、派生或从画像系统读取

当前页面输入包括：

- `user_id`
- `video_id`
- `watch_time`
- `hour_of_day`

其中最核心的单条预测参数是：

- 用户 ID
- 视频 ID
- 观看时长

### 5.4 页面输出

预测成功后，页面需要至少展示以下信息：

- 预测概率 `probability`
- 置信度 `confidence`
- 响应时间 `response_time_ms`
- 模型返回详情 JSON

## 6. 前后端联调设计

第一阶段最关键的是打通前后端闭环

### 6.1 调用链路

调用链路应为：

1. 用户在 `Single Prediction` 页面输入参数
2. 前端组织请求体
3. 前端调用 `POST /predict`
4. 后端接收请求并校验
5. 后端执行预处理
6. 后端调用已加载模型进行推理
7. 后端返回预测结果
8. 前端渲染结果与提示

### 6.2 当前接口契约

请求结构：

```json
{
  "user_id": "user_123",
  "video_id": "video_456",
  "watch_time": 45.0,
  "hour_of_day": 14
}
```

返回结构示例：

```json
{
  "user_id": "user_123",
  "video_id": "video_456",
  "probability": 0.73,
  "confidence": "medium",
  "model_version": "1.0.0",
  "response_time_ms": 12.4,
  "timestamp": "2026-07-17T01:10:56.781514"
}
```



## 7. 技术实现说明

### 7.1 前端实现要点

Single Prediction 页面当前实现包含以下要点：

- 使用文本框输入 `user_id` 和 `video_id`
- 使用滑块输入 `watch_time`
- 使用下拉框选择 `hour_of_day`
- 使用按钮触发预测请求
- 对 `user_id` 和 `video_id` 做基础格式校验
- 对结果进行图形化展示

### 7.2 后端实现要点

Single Prediction 接口当前实现包含以下要点：

- 使用 Pydantic 模型定义请求参数
- 对 `user_id` 与 `video_id` 做规则校验
- 对 `watch_time`、`hour_of_day` 做取值约束
- 预处理阶段构造 DataFrame
- 补齐模型需要的额外字段
- 调用模型预测概率
- 计算置信度并返回结果

## 8. 一阶段的验收标准

第一阶段开发完成后，至少应满足以下验收条件

### 8.1 功能验收

- 页面可正常打开
- 可输入单条预测参数
- 点击按钮后能成功请求后端
- 页面能展示预测结果
- 错误输入时能给出明确提示

### 8.2 技术验收

- 后端服务可独立启动
- 前端服务可独立启动
- `GET /health` 返回正常
- `POST /predict` 可正确返回结构化结果
- 模型文件缺失时后端能输出明确报错

### 8.3 体验验收

- 表单字段命名清晰
- 页面状态反馈明确
- 预测结果展示具有可读性
- 接口调用失败时前端不会直接崩溃

## 9. 测试建议

一阶段虽然范围不大，但仍然需要最低限度测试

### 9.1 手工测试

- 输入合法 `user_id`、`video_id`、`watch_time`，验证是否返回正常结果
- 输入非法 `user_id`，验证页面是否拦截
- 输入非法 `video_id`，验证页面是否拦截
- 关闭后端服务后，验证页面是否提示 API offline
- 将模型文件移走后启动后端，验证是否能发现启动异常

### 9.2 接口测试

建议覆盖：

- `GET /health`
- `POST /predict` 正常请求
- `POST /predict` 非法参数请求
- 边界值测试，如 `watch_time=0`、`hour_of_day=0/23`
